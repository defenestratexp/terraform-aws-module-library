# AWS Compositions

Pre-built architectural patterns combining multiple modules.

_Not yet populated._ Compositions (for example a web-app stack of VPC + ALB + ASG + RDS) are planned; today the library ships individual modules only.
